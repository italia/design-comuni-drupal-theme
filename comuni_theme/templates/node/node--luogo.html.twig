<div class="container" id="main-container">
  <div class="row justify-content-center">
    <div class="col-12 col-lg-10">
      <div class="cmp-breadcrumbs" role="navigation">
        <nav class="breadcrumb-container" aria-label="breadcrumb">
          <ol data-element="breadcrumb" class="breadcrumb p-0">
            <li class="breadcrumb-item">
              <a href="{{ url('<front>') }}">Home</a>
              <span class="separator">/</span>
            </li>
            <li class="breadcrumb-item">
              <a href="{{ url('<front>') }}vivere-il-comune">Vivere il Comune</a>
              <span class="separator">/</span>
            </li>
            <li class="breadcrumb-item">
              <a href="{{ url('<front>') }}luoghi">Luoghi</a>
              <span class="separator">/</span>
            </li>
            {% if content.field_tipo_di_luogo[0] is not empty %}
            <li class="breadcrumb-item">
              {% for item in content.field_tipo_di_luogo["#items"] %}
                <a href="{{ path('entity.taxonomy_term.canonical', {'taxonomy_term': item.target_id}) }}">
                  {{ item|view["#plain_text"]|capitalize }}
                </a>
                {% if not loop.last %} - {% endif %}
              {% endfor %}
              <span class="separator">/</span>
            </li>
            {% endif %}
            <li class="breadcrumb-item active" aria-current="page">
              {{label}}
            </li>
          </ol>
        </nav>
      </div>
    </div>
  </div>
</div>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-12 col-lg-10">
      <div class="cmp-heading pb-3 pb-lg-4">
        <div class="row">
          <div class="col-lg-8">
            {{ title_prefix }}
            {% if node.label %}
              <h1{{ title_attributes.addClass('title-xxxlarge') }} data-element="service-title">
                {{ node.label }}
              </h1>
            {% endif %}
            {{ title_suffix }}
            {% if content.field_nome_alternativo %}
              <h2 class="h4 py-2">
                {{ content.field_nome_alternativo }}
              </h2>
            {% endif %}
            {% if content.field_descrizione_breve %}
              <div class="subtitle-small mb-3" data-element="service-description">
                {{ content.field_descrizione_breve }}
              </div>
            {% endif %}
          </div>
          <div class="col-lg-3 offset-lg-1 mt-5 mt-lg-0">
            {{ drupal_block("social_sharing_buttons_block") }}
            <div class="dropdown">
              <button type="button" id="viewActions" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false"
                class="btn btn-dropdown dropdown-toggle text-decoration-underline d-inline-flex align-items-center fs-0"
                aria-label="vedi azioni da compiere sulla pagina">
                <svg class="icon">
                  <use href="#it-more-items" />
                </svg>
                <small>Vedi azioni</small>
              </button>
              <div class="dropdown-menu shadow-lg" aria-labelledby="viewActions">
                <div class="link-list-wrapper">
                  <ul class="link-list">
                    <li>
                      <a class="list-item" href="javascript:window.print()">
                        <svg class="icon">
                          <use href="#it-print" />
                        </svg>
                        <span>Stampa</span>
                      </a>
                    </li>
                    <li>
                      <a class="list-item" href="#">
                        <svg class="icon">
                          <use href="#it-hearing" />
                        </svg>
                        <span>Ascolta</span>
                      </a>
                    </li>
                    <li>
                      <a class="list-item" href="mailto:?subject={{ node.label }}&body={{ url }}"
                         target="_blank" rel="noopener">
                        <svg class="icon">
                          <use href="#it-mail" />
                        </svg>
                        <span>Invia</span>
                      </a>
                    </li>
                  </ul>
                </div>
              </div>
            </div>
            <div class="mt-4 mb-4">
              {% if content.field_argomenti[0] is not empty %}
                <span class="subtitle-small">Argomenti</span>
                <div class="chip-wrapper mt-2">
                  {% for item in attribute(content.field_argomenti, "#items") %}
                    {% set arg_name = item.entity.name.value %}
                    <a href="{{ path('entity.taxonomy_term.canonical', {'taxonomy_term': item.target_id}) }}" data-element="service-topic">
                      <div class="chip chip-simple chip-primary">
                        <span class="chip-label">{{ arg_name }}</span>
                      </div>
                    </a>
                  {% endfor %}
                </div>
              {% endif %}
            </div>
            <div class="mt-4 mb-4">
              {% if content.field_tipo_di_luogo[0] is not empty %}
                <span class="subtitle-small">Tipo di luogo</span>
                <div class="chip-wrapper mt-2">
                  {% for item in attribute(content.field_tipo_di_luogo, "#items") %}
                    {% set arg_name = item.entity.name.value %}
                    <a href="{{ path('entity.taxonomy_term.canonical', {'taxonomy_term': item.target_id}) }}">
                      <div class="chip chip-simple chip-primary">
                        <span class="chip-label">{{ arg_name }}</span>
                      </div>
                    </a>
                  {% endfor %}
                </div>
              {% endif %}
            </div>
          </div>
        </div>
      </div>
    </div>
    <hr class="d-none d-lg-block mt-2">
  </div>
</div>

<div class="container">
  <div class="row row-column-menu-left mt-4 mt-lg-80 pb-lg-80 pb-40">
    <div class="col-12 col-lg-3 mb-4 border-col">
      <div class="cmp-navscroll sticky-top" aria-labelledby="accordion-title-one">
        <nav class="navbar it-navscroll-wrapper navbar-expand-lg" aria-label="INDICE DELLA PAGINA" data-bs-navscroll>
          <div class="navbar-custom" id="navbarNavProgress">
            <div class="menu-wrapper">
              <div class="link-list-wrapper">
                <div class="accordion">
                  <div class="accordion-item">
                    <span class="accordion-header" id="accordion-title-one">
                      <button class="accordion-button pb-10 px-3" type="button" data-bs-toggle="collapse" data-bs-target="#collapse-one" aria-expanded="true" aria-controls="collapse-one">
                        INDICE DELLA PAGINA
                        <svg class="icon">
                          <use href="#it-expand" />
                        </svg>
                      </button>
                    </span>
                    <div class="progress">
                      <div class="progress-bar it-navscroll-progressbar" role="progressbar" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"></div>
                    </div>
                    <div id="collapse-one" class="accordion-collapse collapse show" role="region" aria-labelledby="accordion-title-one">
                      <div class="accordion-body">
                        <ul class="link-list" data-element="page-index">
                          {% if content.field_descrizione_estesa[0] is not empty %}
                          <li class="nav-item">
                            <a class="nav-link" href="#descrizione_estesa">
                              <span class="title-medium">Descrizione</span>
                            </a>
                          </li>
                          {% endif %}
                          {% if content.field_luoghi_collegati[0] is not empty %}
                          <li class="nav-item">
                            <a class="nav-link" href="#luoghi_collegati">
                              <span class="title-medium">Luoghi collegati</span>
                            </a>
                          </li>
                          {% endif %}
                          {% if content.field_servizi_presenti_nel_luogo[0] is not empty %}
                          <li class="nav-item">
                            <a class="nav-link" href="#servizi_presenti_nel_luogo">
                              <span class="title-medium">Servizi presenti nel luogo</span>
                            </a>
                          </li>
                          {% endif %}
                          {% if content.field_modalita_di_accesso[0] is not empty %}
                          <li class="nav-item">
                            <a class="nav-link" href="#modalita_di_accesso">
                              <span class="title-medium">Modalità di accesso</span>
                            </a>
                          </li>
                          {% endif %}
                          {% if content.field_indirizzo[0] is not empty %}
                          <li class="nav-item">
                            <a class="nav-link" href="#indirizzo">
                              <span class="title-medium">Indirizzo</span>
                            </a>
                          </li>
                          {% endif %}
                          {% if content.field_orario_per_il_pubblico[0] is not empty %}
                          <li class="nav-item">
                            <a class="nav-link" href="#orario_per_il_pubblico">
                              <span class="title-medium">Orario per il pubblico</span>
                            </a>
                          </li>
                          {% endif %}
                          {% if content.field_struttura_responsabile[0] is not empty %}
                          <li class="nav-item">
                            <a class="nav-link" href="#struttura_responsabile">
                              <span class="title-medium">Struttura responsabile</span>
                            </a>
                          </li>
                          {% endif %}
                          {% if drupal_view_result('unita_organizzative_collegate', 'block_1', node.id) %}
                          <li class="nav-item">
                            <a class="nav-link" href="#sede_di">
                              <span class="title-medium">Sede di</span>
                            </a>
                          </li>
                          {% endif %}
                          {% if content.field_punti_di_contatto[0] is not empty %}
                          <li class="nav-item">
                            <a class="nav-link" href="#punti_di_contatto">
                              <span class="title-medium">Contatti</span>
                            </a>
                          </li>
                          {% endif %}
                          {% if content.field_ulteriori_informazioni[0] is not empty %}
                          <li class="nav-item">
                            <a class="nav-link" href="#ulteriori-informazioni">
                              <span class="title-medium">Ulteriori informazioni</span>
                            </a>
                          </li>
                          {% endif %}
                        </ul>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </nav>
      </div>
    </div>

    <div class="col-12 col-lg-8 offset-lg-1">
      <div class="it-page-sections-container">
        {% if content.field_descrizione_estesa[0] is not empty %}
        <section class="it-page-section mb-30">
          <h2 class="title-xxlarge mb-3" id="descrizione_estesa">Descrizione</h2>
          <div class="richtext-wrapper lora">
            {{ content.field_descrizione_estesa }}
          </div>
        </section>
        {% endif %}
        
        {% if content.field_luoghi_collegati[0] is not empty %}
        <section class="it-page-section mb-30">
          <h2 class="title-xxlarge mb-3" id="luoghi_collegati">Luoghi collegati</h2>
          <div class="card-wrapper card-teaser-wrapper">
            {{ content.field_luoghi_collegati }}
          </div>
        </section>
        {% endif %}

        {% if content.field_servizi_presenti_nel_luogo[0] is not empty %}
        <section class="it-page-section mb-30">
          <h2 class="title-xxlarge mb-3" id="servizi_presenti_nel_luogo">Servizi presenti nel luogo</h2>
          <div class="richtext-wrapper lora">
            {{ content.field_servizi_presenti_nel_luogo }}
          </div>
        </section>
        {% endif %}

        {% if content.field_modalita_di_accesso[0] is not empty %}
        <section class="it-page-section mb-30">
          <h2 class="title-xxlarge mb-3" id="modalita_di_accesso">Modalità di accesso</h2>
          <div class="richtext-wrapper lora">
            {{ content.field_modalita_di_accesso }}
          </div>
        </section>
        {% endif %}

        {% if content.field_indirizzo[0] is not empty %}
        <section class="it-page-section mb-30 card-indirizzo-luogo">
          <h2 class="title-xxlarge mb-3" id="modalita_di_accesso">Indirizzo</h2>
          <div class="card-wrapper card-teaser-wrapper">
            {{ drupal_entity('node', node.id, 'card') }}
          </div>
        </section>
        {% endif %}

        {% if content.field_quartiere[0] is not empty %}
        <section class="it-page-section mb-30">
          <h2 class="title-xxlarge mb-3" id="field_quartiere">Quartiere</h2>
          <div class="richtext-wrapper lora">
            {{ content.field_quartiere }}
          </div>
        </section>
        {% endif %}

        {% if content.field_circoscrizione[0] is not empty %}
        <section class="it-page-section mb-30">
          <h2 class="title-xxlarge mb-3" id="field_circoscrizione">Circoscrizione</h2>
          <div class="richtext-wrapper lora">
            {{ content.field_circoscrizione }}
          </div>
        </section>
        {% endif %}

        {% if content.field_orario_per_il_pubblico[0] is not empty %}
        <section class="it-page-section mb-30">
          <h2 class="title-xxlarge mb-3" id="orario_per_il_pubblico">Orario per il pubblico</h2>
          <div class="richtext-wrapper lora">
            {{ content.field_orario_per_il_pubblico }}
          </div>
        </section>
        {% endif %}

        {% if content.field_struttura_responsabile[0] is not empty %}
        <section class="it-page-section mb-30">
          <h2 class="title-xxlarge mb-3" id="struttura_responsabile">Struttura responsabile</h2>
          <div class="card-wrapper card-teaser-wrapper">
            {{ content.field_struttura_responsabile }}
          </div>
        </section>
        {% endif %}

        {% if drupal_view_result('unita_organizzative_collegate', 'block_1', node.id) %}
        <section class="it-page-section mb-30">
          <h2 class="title-xxlarge mb-3" id="sede_di">Sede di</h2>
          <div class="card-wrapper card-teaser-wrapper">
            {{ drupal_view('unita_organizzative_collegate', 'block_1', node.id) }}
          </div>
        </section>
        {% endif %}

        {% if content.field_punti_di_contatto[0] is not empty %}
        <section class="it-page-section mb-30">
          <h2 class="title-xxlarge mb-3" id="punti_di_contatto">Contatti</h2>
          <div class="card-wrapper card-teaser-wrapper">
            {{ content.field_punti_di_contatto }}
          </div>
        </section>
        {% endif %}

        {% if content.field_ulteriori_informazioni[0] is not empty %}
        <section class="it-page-section  mb-30">
          <h2 class="title-xxlarge mb-3" id="more-info">Ulteriori informazioni</h2>
          <div class="richtext-wrapper lora">
            {{ content.field_ulteriori_informazioni }}
          </div>
        </section>
        {% endif %}

      </div>
    
    </div>


  </div>
</div>